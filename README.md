# luogu-saver-backend
后端启动：
安装完MongoDB后，直接进入本项目目录打开终端输入下列命令即可
```
npm install
npm run build
npm run dev
```

Backend startup:

Before install MongoDB, cd this project directionry, open terminal, and enter this command:
```
npm install
npm run build
npm run dev
```

